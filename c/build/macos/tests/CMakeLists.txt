cmake_minimum_required(VERSION 3.13)
project(ciokomb)
set(STATIC_LIBRARY_SUFFIX .a)
set(PROJECT_ROOT_PATH ../../..)
link_directories(${LIB_DIR})
message("TEST:LIB_DIR=${LIB_DIR}")
include(../../${PROJECT_NAME}_tests.cmake)
