cmake_minimum_required(VERSION 3.8)
project("ciokomb")
set(CMAKE_VERBOSE 1)
set(STATIC_LIBRARY_PREFIX "lib")
set(STATIC_LIBRARY_SUFFIX ".a")
set(PROJECT_ROOT_PATH "../..")
include(../${PROJECT_NAME}.cmake)
link_directories(${CMAKE_BINARY_DIR})
#target_link_libraries(${PROJECT_NAME}-Shared {LIB}.dylib)
#target_link_libraries(${PROJECT_NAME}-Static {LIB}.a)
set(TESTS_ROOT_PATH ${PROJECT_ROOT_PATH}/tests)
include_directories(${TESTS_ROOT_PATH})
set(TEST_NAMES)
list(APPEND TEST_NAMES test_string)
list(APPEND TEST_NAMES test_arist)
list(APPEND TEST_NAMES test_pipe)
foreach(testName IN LISTS TEST_NAMES)
  add_executable(${testName} ${TESTS_ROOT_PATH}/${testName}.cpp)
  target_link_libraries(${testName} ${CONFIGURATION}/${STATIC_LIBRARY_PREFIX}${PROJECT_NAME}${STATIC_LIBRARY_SUFFIX})
endforeach()
